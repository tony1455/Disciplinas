#include <stdio.h>

int main(void)
{
    int num,i,cont;

    do
    {
        printf("Informe um numero: ");
        scanf("%d", &num);
    }while(num>0);
        for(i=1;i==num;i++)
        {
            cont++;
            printf("%d")
        }



    return 0;
}
